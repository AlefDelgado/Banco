package GUI;import java.awt.*;import javax.swing.*;/** * * @author alefdelgado */public class GUI extends JFrame{    private JLabel titulo;    private JPanel container;    private CardLayout cards;        // Paleta de colores UAEMex    public static final Color VERDE_UAEM = new Color(60, 104, 71);    public static final Color VERDE_UAEM_CLARO= new Color(60, 122, 84);    public static final Color VERDE_UAEM_SECUNDARIO = new Color(60, 75, 36);        // Colores genericos    public static final Color AZUL_PRINCIPAL = new Color(13, 71, 161);     // Azul oscuro    public static final Color AZUL_SECUNDARIO = new Color(25, 118, 210);   // Azul medio    public static final Color AZUL_CLARO = new Color(66, 165, 245);        // Azul claro    public static final Color AZUL_MUY_CLARO = new Color(227, 242, 253);   // Azul muy claro    public static final Color VERDE_EXITO = new Color(46, 125, 50);        // Verde    public static final Color ROJO_PELIGRO = new Color(198, 40, 40);       // Rojo    public static final Color GRIS_TEXTO = new Color(66, 66, 66);          // Gris oscuro    public static final Color GRIS_CLARO = new Color(245, 245, 245);       // Gris muy claro        public GUI()    {        // titulo del programa        super("BANCO CUUT");        setLayout(new BorderLayout());                // Invocacion de la barra de menu        JMenuBar bar = createMenuBar();        setJMenuBar(bar);                // Invocar encabezado        JPanel header = createHeader();        add(header, BorderLayout.NORTH);                // Contenedor con cartas de paginas        cards = new CardLayout();        container = new JPanel(cards);        container.setBackground(Color.LIGHT_GRAY);                container.add(new Welcome(this), "Welcome");        container.add(new LoginPanel(this), "Login");                add(container, BorderLayout.CENTER);                // Invocar pie de pagina        JPanel footer = createFooter();        add(footer, BorderLayout.SOUTH);                revalidate();        repaint();    }        private JMenuBar createMenuBar()    {        // Creacion y estetica de la barra de menu        JMenuBar bar = new JMenuBar();        bar.setBackground(Color.GRAY);        bar.setOpaque(true);        bar.setBorder(BorderFactory.createEmptyBorder(5,10,5,10));                // Crear el menu 1        JMenu menu = new JMenu("Menu");        menu.setForeground(Color.BLACK);        menu.setBorder(BorderFactory.createEmptyBorder(5, 10, 5, 10));                // Crear el submenu "Acerca de"        JMenuItem aboutThis = new JMenuItem("Acerca del Sistema");        aboutThis.setForeground(Color.BLACK);        aboutThis.setBorder(BorderFactory.createEmptyBorder(5, 10, 5, 10));        aboutThis.addActionListener(e ->        {            createPersonalizedDialogue("BANPOTRO",                    "Banca Electronica \n" +                    "¡SISTEMA NO OFICIAL! \n"+                    "Autor: Alef Delgado",                    JOptionPane.INFORMATION_MESSAGE);        });                // Crear el submenu para salir del sistema        JMenuItem exit = new JMenuItem("Salir");        exit.setBorder(BorderFactory.createEmptyBorder(5, 10, 5, 10));        exit.setForeground(Color.BLACK);        exit.addActionListener(e ->        {            int answer = JOptionPane.showConfirmDialog(                    this,                    "Seguro que desea abandonar el sistema?",                    "ADVERTENCIA!",                    JOptionPane.YES_NO_OPTION,                    JOptionPane.QUESTION_MESSAGE);            if (answer == JOptionPane.YES_OPTION)            {                System.exit(0);            }                    });                menu.add(aboutThis);        menu.addSeparator();        menu.add(exit);                bar.add(menu);                return bar;    }        // Creacion y configuracion del encabezado    private JPanel createHeader()    {        JPanel header = new JPanel();        header.setLayout(new BorderLayout());        header.setBackground(VERDE_UAEM);        header.setPreferredSize(new Dimension(0, 80));                titulo = new JLabel("BANPOTRO", SwingConstants.CENTER);        titulo.setFont(new Font("Segoe UI", Font.BOLD, 28));        titulo.setForeground(Color.WHITE);                header.add(titulo, BorderLayout.CENTER);                return header;    }        // Creacion y configuracion del pie de pagina    private JPanel createFooter()    {        JPanel footer = new JPanel();        footer.setLayout(new BorderLayout());        footer.setBackground(VERDE_UAEM);        footer.setPreferredSize(new Dimension(0,80));                JLabel lblfoot = new JLabel("2026 UAEMex - BANCA ELECTRONICA", SwingConstants.CENTER);        lblfoot.setForeground(Color.WHITE);        lblfoot.setFont(new Font("Segoe UI", Font.PLAIN, 11));                footer.add(lblfoot, BorderLayout.CENTER);                return footer;    }        public void showCards(String panelName)    {        cards.show(container, panelName);                switch (panelName)        {            case "login":                titulo.setText("Iniciar Sesión");                break;            default:                titulo.setText("BANPOTRO");        }    }        // Disenio de boton principal    public static JButton createPrimaryButton(String text)    {        JButton button = new JButton(text);        button.setBackground(VERDE_UAEM);        button.setOpaque(true);        button.setForeground(Color.WHITE);        button.setFont(new Font("Segoe UI", Font.BOLD, 13));        button.setFocusPainted(false);        button.setBorderPainted(false);        button.setCursor(new Cursor(Cursor.HAND_CURSOR));        button.setBorder(BorderFactory.createEmptyBorder(10, 20, 10, 20));                // Efecto hover        button.addMouseListener(new java.awt.event.MouseAdapter() {            public void mouseEntered(java.awt.event.MouseEvent evt) {                button.setBackground(VERDE_UAEM_CLARO);                button.setOpaque(true);            }            public void mouseExited(java.awt.event.MouseEvent evt) {                button.setBackground(VERDE_UAEM);                button.setOpaque(true);            }        });                        return button;    }        // Metodo auxiliar para dialgo personalizado    public void createPersonalizedDialogue(String title, String message, int type)    {        JOptionPane.showMessageDialog(this, message, title, type);    }    }